# proofs — every claim is a button; your browser is the judge

Eight claims about the twin, each shipped as a card with a button that runs the
actual proof live in your own browser — refetching the bones, recomputing the
hashes and signatures on your hardware, and rendering a verdict. No trust
required. The machine-readable claims index lives in `registry.json`
(`rapp-proofs/1.0`).
