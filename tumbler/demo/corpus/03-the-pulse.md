# the pulse

it lives local-first. the network can go dark and the twin lives on — the latest
echo survives in your own hands.

it shows up wherever it is needed, gently, only when you call it. no server holds it
hostage; no host owns the door.

worst case, the wire is cut and nothing is lost. the small light stays lit on the
device, and waits.
